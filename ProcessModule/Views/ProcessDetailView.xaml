<UserControl x:Class="ProcessModule.Views.ProcessDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProcessModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


   
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        

        <Label Grid.Column="0" Grid.Row="0" Content="Name Process"/>
        <Label Grid.Column="1" Grid.Row="0" Content="{Binding SelectedProcess.Name}"/>

        <Label Grid.Column="0" Grid.Row="1" Content="CPU"/>
        <Label Grid.Column="1" Grid.Row="1" Content="{Binding SelectedProcess.CPU}"/>

        <Label Grid.Column="0" Grid.Row="2" Content="Memory"/>
        <Label Grid.Column="1" Grid.Row="2" Content="{Binding SelectedProcess.Memory}"/>

       
    </Grid>
</UserControl>
