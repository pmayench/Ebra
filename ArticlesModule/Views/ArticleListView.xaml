<Window x:Class="ArticlesModule.Views.ArticleListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ArticlesModule.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ArticleListView">

    <Grid>
        <ListView ItemsSource="{Binding Articles}"
          SelectedItem="{Binding SelectedArticle}" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" Width="80" DisplayMemberBinding="{Binding id}"/>
                    <GridViewColumn Header="Name" Width="160" 
                            DisplayMemberBinding="{Binding name}" />
                    <GridViewColumn Header="Description" Width="80" DisplayMemberBinding="{Binding description}"/>
                    <GridViewColumn Header="Price" Width="80" DisplayMemberBinding="{Binding price}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
